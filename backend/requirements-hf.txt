# Dépendances pour le déploiement HuggingFace Spaces (sans CUDA/GPU).
# PyTorch CPU est installé séparément dans le Dockerfile via l'index CPU de PyTorch.

# ── API ─────────────────────────────────────────────────────────────────────
fastapi==0.115.14
uvicorn[standard]==0.34.3
starlette==0.46.2
orjson==3.11.8
httpx==0.28.1
httpx-sse==0.4.3
anyio==4.13.0

# ── Config / validation ─────────────────────────────────────────────────────
pydantic==2.13.3
pydantic-settings==2.14.0
pydantic_core==2.46.3
python-dotenv==1.2.2

# ── LangChain ───────────────────────────────────────────────────────────────
langchain==1.2.15
langchain-core==1.3.2
langchain-community==0.4.1
langchain-text-splitters==1.1.2
langsmith==0.7.36
langchain-ollama==1.1.0

# LangChain integrations (non incluses dans le freeze d'origine)
langchain-huggingface>=0.1.0
langchain-qdrant>=0.2.0
langchain-mistralai>=0.2.0   # Mistral API pour la production

# ── Vector store ─────────────────────────────────────────────────────────────
qdrant-client==1.12.2

# ── Cache ────────────────────────────────────────────────────────────────────
redis==5.3.1

# ── Embeddings / ML ──────────────────────────────────────────────────────────
# torch est installé via le Dockerfile (wheel CPU pour réduire la taille)
sentence-transformers==3.4.1
transformers==4.57.6
tokenizers==0.22.2
huggingface_hub==0.36.2
numpy==2.4.4
scipy==1.17.1

# ── Document loaders ─────────────────────────────────────────────────────────
pypdf==5.9.0
docx2txt==0.8

# ── Utilitaires ──────────────────────────────────────────────────────────────
tenacity==9.1.4
